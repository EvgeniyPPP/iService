services:
  iservice:
    build: .
    container_name: iservice-app
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - JWT_SECRET=iservice_secret_key_2024
    volumes:
      - ./backend/iservice.db:/app/backend/iservice.db
    restart: unless-stopped